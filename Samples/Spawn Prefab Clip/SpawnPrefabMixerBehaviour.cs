using TimelineKit;

/// <summary>
/// Mixer behaviour for SpawnPrefabTrack.
/// SpawnPrefabBehaviour handles its own lifecycle per-clip, so the mixer
/// has no blending work to do.
/// </summary>
public class SpawnPrefabMixerBehaviour : PlayableBehaviourEx { }
